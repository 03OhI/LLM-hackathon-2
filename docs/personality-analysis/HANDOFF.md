# 넘길 것 — 최종본 목록 (2026-08-28)

전부 `RAG/` 안에 있습니다. 아래 그대로 압축해서 보내면 됩니다.

---

## 1. 코드 — 이것만 있으면 돌아갑니다

| 파일 | 역할 |
|---|---|
| `chemistry_v2.py` | 축 점수 · 등급 · 마찰 판정 (핵심) |
| `faultline.py` | 갈라짐 진단 (Fau · breadth · ASW) |
| `cases.py` | 16개 케이스 문단 · 조치 문장 · 논문 근거 |
| `likert.py` | 24문항 응답 → 축 점수 변환 |
| `evidence_map.json` | **논문 근거 정본** |
| `mkmap.py` | `evidence_map.json` 생성 |
| `sync_evidence.py` | `evidence_map.json` → `cases.py` 반영 |
| `frictions.py` / `frictions.json` | 마찰 규칙 13종 추출 결과 |
| `verify_fixes.py` | 수용 테스트 — **받는 쪽에서 이걸 먼저 돌리세요** |

```
python verify_fixes.py     # 17/17 나와야 정상
```

**근거를 고칠 때는 `cases.py` 를 직접 만지지 마세요.**
`mkmap.py` 를 고치고 `python mkmap.py && python sync_evidence.py` 를 돌립니다.
두 곳에서 손으로 관리하다 이미 두 번 갈라졌습니다.

## 2. 문서

| 파일 | 내용 |
|---|---|
| `설계_확정_보고서.docx` | 본 보고서 (§9.1 · §4.3 · §5.4 갱신됨) |
| `ALPHA_RESULT.md` | 내적 일관성 측정 n=22 — **§9.1 `[미측정]` 을 채운 기록** |
| `FACTOR_ANALYSIS_SUMMARY.md` | 역채점 검증 — 시뮬과 실측이 정반대였다 |
| `GRADE_V2.md` | 등급 규칙 (총점 → "0.5 미만인 축 개수") |
| `SCORE_DRIVE.md` | 주도성 인원수 집계 규칙 |
| `DEFENSE.md` | 판정 불가를 만점으로 주던 결함과 조치 |
| `PARTIAL_SPLIT.md` | 59% 뭉치를 4개로 쪼갠 근거 |
| `EVIDENCE_MAP.md` | 케이스↔논문 대조표 (사람이 읽는 판) |
| `SURVEY24.md` · `REVERSE_SCORING_ANALYSIS.md` | 설문 설계 · 한계 |
| `responses22.jsonl` | 실제 응답 22건 원자료 |
| `주제선정/발표_문제정의_주제선정_2장.docx` | 발표용 문제 정의 |

## 3. 지금 상태 (숨기는 것 없이)

**끝난 것**

- 수용 테스트 17/17
- 마찰 13종 → 케이스 13건 전부 연결, 미연결 0
- 케이스 16건, 문단 구별도 최대 0.308 (기준 0.75)
- 근거 구멍 0건 — 화면에 출처가 뜨는 7건은 전부 원문 대조 완료

**남은 것 — 발표에서 먼저 말해야 하는 것**

- `[검증됨 — 예상과 반대]` 설문 신뢰도가 낮습니다. α .35~.56, 갈등대응은 −.50.
  정방향+역채점 평균 합이 6.0 이어야 하는데 7.27~7.70 입니다. 원인은 표본이 아니라 문항입니다
  (17건과 22건이 같은 값을 줍니다). 고친 문항안은 `ALPHA_RESULT.md` §5.
- `[미검증]` 요인분석은 아직 못 합니다. 30~60명이 필요합니다.
- `[팀 판단]` 케이스 16건 중 **8건은 논문 근거가 없는 우리 규칙**입니다.
  `evidence_status` 에 `[규칙]` 로 표시돼 있습니다. "네 축 전부 논문 근거"라고 말하면 안 됩니다.
- `TEAM_ISOLATED` 의 Meyer & Glenz(2013)는 **측정 방법 문헌**입니다.
  화면 출처로 쓰지 않도록 막아뒀습니다. 발표에서 근거로 인용하지 마세요.

**⚠️ 보내기 전에 한 번**

`RAG/` 에 `~$_확정_보고서.docx` 락 파일이 남아 있습니다. 누군가 Word 로 보고서를 열어 두고 있다는 뜻이고,
그 창에서 저장하면 갱신된 §9.1 이 덮입니다. **Word 를 모두 닫고 파일 크기·수정 시각을 확인한 뒤 압축하세요.**
